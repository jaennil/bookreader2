apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: bookreader-data
  labels:
    app: bookreader
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: local-path
  resources:
    requests:
      storage: 5Gi
